name: macroenv
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11  # Kompatible Version für lupa
  - pyqt=6.*  # Updated to PyQt6
  - evdev>=1.4.0
  - lupa>=1.10
  - pip
  - pyperclip
